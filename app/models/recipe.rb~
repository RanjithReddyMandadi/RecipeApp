class Recipe < ActiveRecord::Base
  attr_accessible :description, :ingredients, :preptime, :title
  belongs_to :user
  after_create :update_user_mailer
  def update_mailer(user)
    get_user_mail(user)
  end
end
